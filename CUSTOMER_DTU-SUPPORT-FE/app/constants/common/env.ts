export enum NodeEnv {
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export enum EnvFile {
  EXAMPLE = '.env.example',
  DEVELOPMENT = '.env.development',
  STAGING = '.env.staging',
  PRODUCTION = '.env.production',
}
